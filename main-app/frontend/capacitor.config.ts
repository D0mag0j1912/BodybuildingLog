import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'bodybuildingLog',
    webDir: 'www',
    bundledWebRuntime: false,
};

export default config;
